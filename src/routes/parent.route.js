const express = require("express")
const router = express.Router();
const parentController=require("../controllers/parent.controllers")
// router.post("/link",parentController.postParent)
// router.get("/children",parentController.getParent)
// router.get("/:studentId",parentController.getParent)
// router.put("/permissions",parentController.updateParent)
module.exports=router;